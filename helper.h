#include <bits/stdc++.h>
#include "token.h"
using namespace std;
vector<Token> StringToTokenList(string inputString); //untuk melakukan stripping token dari string yang mengandung spasi

//debugger
void PrintTokenList(vector<Token> TokenList);

